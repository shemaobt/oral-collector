import 'package:flutter/material.dart';

// Este arquivo está sob lib/core/theme/ → isento. As linhas abaixo NÃO podem
// sinalizar (sem // expect_lint); se sinalizarem, o run falha (lint inesperado).
abstract class FixtureTokens {
  static const Color brand = Color(0xFF89AAA3);
  static const Color white = Colors.white;
}
